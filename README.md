# Build-your-own-Redis
This repo contains all files needed for my own version of Redis. This was done as practice.
As step 1, we first start off with building a basic TCP server which can get connections from differet clients.
